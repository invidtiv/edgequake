# LENS — Marketing / Credibility (SPEC-090)

Latency without recall and without corpus size is not a performance result.

## Required public claims shape

```
hybrid_query_p95_ms = …
recall_at_10 = …
corpus_chunks = …
hardware = …
pgvector / PG versions = …
```

Item 24 (falsifiable scaling harness) is the credibility fix. Prefer quiet engineering wins (F-090-01, F-090-16) over aspirational concurrency numbers.
